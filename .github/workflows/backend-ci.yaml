name: Backend CI

on: [ push ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set environment variables
        run: |
          echo "DJANGO_SECRET_KEY=%)*j%&wt_p(9e#t#2+9!uf@!8n3352q9e+!m%l4u)odmmusdc!" >> .env
          echo "DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2" >> .env
          echo "EMAIL_HOST=smtp.gmail.com" >> .env
          echo "EMAIL_PORT=587" >> .env
          echo "EMAIL_USE_TLS=True" >> .env
          echo "EMAIL_HOST_USER=lennient.enterprise@gmail.com" >> .env
          echo "EMAIL_HOST_PASSWORD=yltf khuu xjdk vrah" >> .env
          echo "DJANGO_CONFIGURATION=Local" >> .env
          echo "DEFAULT_FROM_EMAIL=lennient.enterprise@gmail.com" >> .env
          echo "RENDER_EXTERNAL_HOSTNAME=your_render_external_hostname" >> .env
          echo "DJANGO_PAGINATION_LIMIT=10" >> .env
          echo "TF_CPP_MIN_LOG_LEVEL=2" >> .env
          echo "TF_ENABLE_ONEDNN_OPTS=0" >> .env

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements/requirements_api.txt
          pip install -r requirements/requirements_test.txt

      - name: Run Migrations
        run: |
          python manage.py makemigrations
          python manage.py migrate
          python manage.py loaddata backup.json

      - name: Run Tests
        run: |
          coverage run -m pytest
          coverage html

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: htmlcov/
